spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shike?useSSL=false&serverTimezone=UTC
    username: root
    password: 2003888
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
